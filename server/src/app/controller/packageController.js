class packageController {
    // Thêm dữ liệu hàng gửi
    addPackage(req, res, next) {
        
    }

    // Xóa dữ liệu hàng gửi
    removePackage(req, res, next) {
        
    }

    // Lấy dữ liệu hàng gửi
    getPackages(req, res, next) {
        // Your code here
    }

    // Cập nhật trạng thái hàng gửi
    updatePackageStatus(req, res, next) {
        // Your code here
    }

    // Thống kê hàng 
    statistics(req, res, next) {
        // Add your code here to perform the statistics
    }
}

module.exports = new packageController();